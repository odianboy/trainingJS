'use strict'

/*
Проверка синтаксиса
Каким будет результат выполнения этого кода?

let user = {
  name: "Джон",
  go: function() { alert(this.name) }
}

(user.go)()
 */

// Пропущена точка с запятой после объявления объекта.

/*
let user = {
  name: "Джон",
  go: function() { alert(this.name) }
};

user.go()
 */
