'use strict'

/*
Объекты-константы?
Можно ли изменить объект, объявленный с помощью const? Как вы думаете?

const user = {
  name: "John"
};

// это будет работать?
user.name = "Pete";
*/

// Ответ "Да", изменить нельзя только имя переменной (user), а то, что внутри нее можно